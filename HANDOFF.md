# 引き継ぎ書: 架空の商店街（FreeHP写真素材）

**今すぐ分かること**: 公開URL https://ryoseiimai.github.io/kakuu-shotengai/ 、素材5点は公開済み・検品済み。次に判断すべきことは「FreeHPの見本に実際に差し込むか」だけ。エンジン改修は不要と分かっている。

---

## これは何

2026-09-22〜23、クリップ履歴に残っていた画像生成プロンプト（「ミニけし」消しゴムパッケージ）を起点に、実写にしか見えないAI生成の商品・店内写真を作り、FreeHP（無料ホームページの会社）向けの素材として一般公開した。

## 状態: 公開済み・検品済み・完了

- 公開URL: https://ryoseiimai.github.io/kakuu-shotengai/ （誰でも閲覧可）
- GitHubリポジトリ: github.com/Ryoseiimai/kakuu-shotengai （個人アカウント・公開・GitHub Pagesで配信）
- 手元の作業ディレクトリ: `~/dev/2026-09-23-freehp-photo-assets/`
  - `index.html` = 公開ページ本体
  - `images/` = 公開用JPEG5点
  - `originals/` = 生成時のPNG原本5点
  - `prompts/RECIPE.md` = 技法メモ（後述）
  - `README.md`

素材5点（すべて実在しない架空の店・商品）:

1. **ミニけし**（文具・雑貨・駄菓子屋向け）— 透明OPP袋に入った大量のミニ消しゴム、指先が1つをつまもうとしている構図
2. **そらまめ焙煎所**（珈琲店・焙煎所向け）— クラフト紙の珈琲豆袋とこぼれた焙煎豆
3. **クリームソーダ**（喫茶店・カフェ向け）— 古い喫茶店のメロンクリームソーダ、結露と溶けかけのアイス
4. **鋏とコーム**（美容室・理容室向け）— タオルの上の鋏とコーム、細かい傷と毛
5. **施術室**（整体・整骨・治療院向け）— 施術ベッドとタオル、人物なし

表示検品は実Chromeでデスクトップ幅・ダークモードを目視確認済み。画像5点とも配信サーバでHTTP 200・Content-Type: image/jpegを確認済み。スマホ幅も2026-09-23 03:40に確認済み。当初はviewport指定が抜けていてスマホでPC幅（980px）に縮小表示されていたため、doctype・charset・viewportを追加した（83b78fa）。修正後は390px・320px幅ともページ全体の横スクロールなし。「作り方」の表だけは `.tbl-scroll` の中で横にスクロールする（意図どおり）。

## 技法（レシピ）

詳細は同ディレクトリの `prompts/RECIPE.md`。核心だけ書くと:

- AIっぽさは「きれいすぎること」から来る。「リアルに」ではなく、現物にある物理的な欠陥を具体的に指定すると効く（印刷ズレ・インクのムラ・完全マットな素材・微細な粒子や粉・ビニールのシワや曇りや静電気・結露・水たまり・溶けかけ、など）
- AIの既定は「良く見せる」方向に寄るので、`avoid: glossy plastic rendering, luxurious finish, perfectly flawless printing, studio perfection, CGI gloss` のような禁止指定を必ず添えないと中央値に戻ってしまう
- 構図で一番効いたのは「指先が1つをつまもうとしている」演出。撮影された商品ではなく生活の中の物に見える
- **罠**: 架空の会社名を名指しで指定しないと、AIがパッケージに実在する企業名を書いてしまう（1回目の生成で実在の玩具メーカー名が入り、作り直した）。架空社名を明示し、実在名を使わないよう禁止指定も添える必要がある
- 生成には `mcp__openai-chat__generate_image` の `model: "gpt-image-2"`、`quality: "high"` を使用。1枚あたり2〜3分かかる

## FreeHP本体との接続: 未着手・本人判断待ち

FreeHPのエンジン本体（正本は `~/dev/2026-08-03-free-hp-engine-isolated`、Cloudflare Worker、freehp.jpで本番稼働）を調査した結果、以下が判明している。

- FreeHPのサイトは「写真なしで成立する設計」。5つの骨格（デザインテンプレート）と色パレットだけでページが組まれ、写真は店主が自分でアップロードする1枚だけの任意項目
- 見本の写真受け口は `worker/src/domain/render/parts.ts` の `buildPhotoInfo` 関数（369行目付近に定義される `PHOTO_DATA_URI_PATTERN` という正規表現）で、`data:image/jpeg;base64,...` 形式のみを受け付ける。外部URL画像やプレースホルダは使わない設計
- 写真データの上限は `worker/src/domain/validate.ts` の `PHOTO_MAX_LENGTH`（200万文字）。今回作った5枚はbase64化しても45万〜88万文字程度なので、この上限には収まる
- **つまりエンジン側の改修は不要で、生成した写真をbase64のdata URIに変換して見本のphotoフィールドにそのまま渡せば表示できるはず**（実際に見本へ差し込んで試す検証はまだ行っていない）
- 一方で「業種ごとに写真をあらかじめプールしておく素材ライブラリ」という概念はリポジトリのどこにも存在しない。もしこの5点を見本生成に自動で組み込みたいなら、新規に設計・実装する必要がある
- Cloudflareのアカウント（account_id）が会社（telewor）か個人（wrp）のどちらに属すかは未確認のまま残っている

## 次の一手（本人が決めること）

FreeHPの見本にこの5点を実際に差し込むかどうかは本人の判断待ち。GOと決まった場合に想定される作業:

1. 業種と写真の対応を決める（例: 喫茶店の骨格にはクリームソーダ、美容室の骨格には鋏とコーム）
2. 見本生成のどこかの工程で、対応する写真をbase64のdata URIに変換してphotoフィールドに注入する経路を作る（エンジンの検証ロジック自体は変更不要という見立て）
3. 実際の骨格・配色との組み合わせを見本として出し、見た目の相性を目視確認する

## 関連情報

- 技法メモの恒久メモリ: `reference_real_looking_product_photo_recipe.md`（`~/.claude/projects/-Users-ryoseiworld/memory/` 配下）
- 依頼台帳: irai R1803（このプロジェクトの依頼、完了済み扱い）
- FreeHPの見本品質基準: memory `feedback-freehp-sample-quality-bar.md`（「シンプルでいいがダサいは絶対NG」）
- AI生成物を顧客本番に載せない原則: memory `feedback_no_ai_copy_to_client_production.md`（この5点は見本用途に限る、実在店の本番には使わない）

---

作成日: 2026-09-23
